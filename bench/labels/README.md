One JSONL file per rule (matching the rule's file name, e.g. `name-matches-body.jsonl`). See `../README.md` for the line format and labeling goals.

Currently seeded with 10 hand-written cases for `name-matches-body`; the remaining 40 per rule, and the other rules' files, are labeled by hand from `bench/repos.json` once those repos are pinned.
